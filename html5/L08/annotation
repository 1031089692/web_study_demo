contentEditable属性：True和False决定元素是否可编辑。未指定True和False时，则由继承的状态来决定。
designMode属性：用来指定整个页面是否可编辑。当页面可编辑时，该页面的所有支持上下文的contentEditable属性都变成可编辑状态。--只能js编辑
hidden属性：决定是否渲染(是否显示)
spellcheck属性：对于input和textarea属性。对于输入的内容进行拼写和语法检查.
tabindex属性：有该属性的可以tab顺序切换。